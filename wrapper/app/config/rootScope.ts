declare module MobileWrapper.Config{
    interface IRootScope extends ng.IRootScopeService {
        menu: string,
        mySplitter: any;
        playerSongCover: string;
        playerSongId: string;
    }
}